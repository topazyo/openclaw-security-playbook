# See: docs/guides/02-credential-isolation.md
# Credentials Configuration Template

---
credentials:
  storage: "os_keychain"  # Use OS keychain instead of plaintext

  keychain:
    service_prefix: "ai.openclaw"
    require_touch_id: true
